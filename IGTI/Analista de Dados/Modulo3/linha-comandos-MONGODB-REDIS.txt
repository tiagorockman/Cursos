/*********** BANDO DE DADOS MODELO DOCUMENTO ****************/
-- Mais comandos https://docs.mongodb.com/manual/reference/method/

db 						--> lista bases
use igti 				--> cria ou muda para a base igti
---Linha abaixo faz insert do documento na base (após insert retorna o id do registro)
db.igti.insertOne({ nome:'charles', sobrenome:'fortes', email:'teste@teste'}); 
db.igti.insertOne({ nome:'bill', sobrenome:'jobs', cel:'31313131'}); 
db.igti.insertOne({ nome:'luke'});
db.igti.insertOne({ nome:'spock'});
db.igti.insertOne({ nome:'silvio'});
db.igti.insertOne({ nome:'goku'});
db.igti.insertOne({ nome:'fly'});
db.igti.insertOne({ nome:'xuxa'});

---Linha abaixo busca todos documentos na base
db["igti"].find()
--Busca por atributos
db["igti"].find({ nome: "charles"})
db["igti"].find({ nome: "bill"})
db["igti"].find({ nome: "Jobs"})
db["igti"].find({ nome: "bill", cel:'31313131' })
db["igti"].findOne()			--> Busca o primeiro registro
db["igti"].find({nome: /l/}) 	--> Busca todos nomes com letra L
db["igti"].find().limit(3)		--> Traz os 3 primeiros
db["igti"].find().limit(3).skip(2)	--> Traz os 3 pulando os 2 primeiros

--delete
db.igti.deleteOne({nome: 'xuxa'})

--update			where			set
db.igti.updateOne({nome: 'bill'}, {$set:{nome:'steve'}})

/*********** BANCO DE DADOS MODELO CHAVE VALOR - BANCO REDIS ****************/
--Mais comandos https://redis.io/commands/set
SET "A" "VALOR QUALQUER" 		--> Cria chave a com o texto
GET "A" 						--> Seleciona o texto da chave A
set c 1							--> Define chave C com valor 1
INCR c							--> incrementa 1 
get c							--> exibe o valor de c. c ==2
INCRBY c 10						--> incrementa 10. c== 12
exists c						--> verifica existencia da chave
sadd listaA "a" "b" "c"			--> cria lista chave listaA
sembers listaA					--> exibe conteudo da lista
spop listaA						--> remove primeiro item da lista
hmset funcionarios nome "charles" sobrenome "fortes"  	--> cria lista complexa
hmget funcionarios nome 								--> retorna nome do funcionario
expire a w2						--> expira a chave a em 2 segundos
set "abc" "um novo valor" ex 5 	--> cria chave abc e expira em 5 segundos
bgsave							--> salva os dados criados

/*********** BANCO DE DADOS MODELO COLUNAR (FAMILIA DE COLUNA) - BANCO KASSANDRA ****************/
/* LINK DOWNLOAD - https://academy.datastax.com/planet-cassandra//companies/ */

--cria keyspace igti
CREATE KEYSPACE igti WITH replication = 
	{'class': 'SimpleStrategy', 'replication_cactor' :1}
	
--descreve os objetos criados dentro da base igti
describe igti

--descreve keyspace
describe keyspace

--create table
create table pessoas( id int Primary key, nome Text, sobrenome Text);

--descreve tabelas
describe tables

--seleciona
select * from pessoas

